//                            ​‌‌‍⁡⁢⁣⁢EXO 1​⁡
function neww(a, b) {
    const result = parseFloat((a + b).toFixed(2));
    console.log(result);
  }
  
  neww(2, 3); //
  neww(10, -5); //

//                    ⁡⁣⁢⁢ explaination⁡
// ⁡⁢⁢⁢- We start by defining a function called   new  . The function takes two parameters,   a   and   b  .
// - Inside the function, we create a new variable called   result   and set it equal to the sum of   a   and   b  .
// - We then use the   console.log   method to log the value of   result   to the console.
// - Finally, we call the   add   function twice with different arguments to demonstrate how it works.

//                ⁡⁢⁣⁢​‌‌‍  EXO 2​⁡ 

function subtractValues(num1, num2) {
  console.log(num1 - num2);
}

subtractValues(10, 5);  
